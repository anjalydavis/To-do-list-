# My Day — To-Do App

A simple animated to-do list built with React + Vite.

## Setup

```bash
npm install
npm run dev
```

## Usage

- **Add task** — type in the input and press `Enter` or click `+`
- **Complete task** — click the circle checkbox, it slides off
- **Delete task** — hover over a task and click `×`
- **Filter** — use the category pills to filter by Work, Personal, Health, Learning
- **Dark mode** — toggle the ☀️ / 🌙 switch in the top right

## Built With

- React 18
- Vite
- CSS animations (no libraries)
